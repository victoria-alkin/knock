# Expo HAS CHANGED

This project uses Expo SDK 54 (the stable release, for Expo Go compatibility). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code. Do not upgrade to a preview SDK (e.g. 57) — Expo Go can't run it.
